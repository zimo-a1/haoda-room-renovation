# 学生操作手册：从一个想法到产品上线，你要做的每一步

> 你不需要懂代码。你只需要有一个产品想法（有 PRD 更好，没有也行），会打字、会截图。
> 用什么 AI 模型都可以：Claude、GPT、DeepSeek、Kimi 都行。关键不是模型，而是**工具必须能操作你的电脑**。

## 一、准备（只做一次，约 15 分钟）

1. **装一个"编码 AI 工具"**。它和网页聊天的区别是：它能在你电脑上装东西、跑程序、改文件、帮你上线。网页版 AI（只能聊天的那种）做不到这些，不能用。
   常见选择：Claude Code、Cursor、Codex CLI 等。装哪个都行，装好后把你的模型 Key 或账号配进去（工具自己的说明会教）。
2. **把这个文件夹放到电脑上**（收到的是压缩包就先解压）。不要改里面任何文件。
3. **放 PRD（可选）**：有 PRD 的，把文件放进 `docs/PRD/` 文件夹，Word、Markdown、PDF 都可以。没有 PRD 的，跳过，AI 会带你问出来。

## 二、第一次对话

1. 用编码 AI 工具**打开这个文件夹**（工具里一般叫"打开项目"或"打开文件夹"）。
2. 在对话框里发这句话（复制即可）：

   ```text
   请读取项目根目录的 AGENTS.md，按其中"启动流程"开始。
   ```

3. AI 会根据情况回你三种之一：

   | 你的情况 | AI 会做什么 | 你怎么回 |
   | --- | --- | --- |
   | 没放 PRD | 一次问你不超过 5 个问题，每个附示例，帮你把 PRD 写出来 | 用自己的话答；不知道就说"不确定" |
   | 放了 PRD | 先检查电脑环境，再给你一份《PRD 补全清单》：哪些要你回答（不超过 5 个，每个有推荐），哪些它先用默认值 | 逐个答，或直接说"按推荐" |
   | 之前做过、这次接着做 | 一句话告诉你上次做到哪、这次做什么 | 说"继续" |

4. 之后每一步 AI 都会说清"下一步你要做什么"。你看不懂它的问题时，回一句"看不懂，用产品语言再说一遍"。

## 三、整个过程会经历 6 个阶段，每个阶段你只做这些事

| 阶段 | AI 在做什么 | 你要做的 | 大概要你花的时间 |
| --- | --- | --- | --- |
| 0 PRD 体检 | 检查 PRD 缺什么，问你 | 回答问题（可以"按推荐"） | 半小时 |
| 1 技术适配 | 出一份《技术适配声明》 | 只确认三件事：它理解得对不对；有没有多出你没要求的花钱项、平台、范围；被暂缓的功能影不影响这次验收 | 10 分钟 |
| 2 后端 MVP | 写核心功能、跑测试 | 去申请模型 Key 填到它指定的位置；它给你 5 到 10 条真实生成结果，你打分；照它的清单点一遍验收 | 每轮 1 小时 |
| 3 正式前端 | 做界面 | 给它设计参考或说"中性风格"；看它做的第一页提意见；用手机走一遍 | 每轮 1 小时 |
| 4 上线 | 部署到云 | 注册云账号、实名认证、开权限、给密钥；它给"照着点"步骤你就点；拿到网址后按验收清单点一遍 | 半天（认证要等几天，AI 会提前提醒你去办） |
| 5 收尾 | 整理文档 | 按它的提示把云密钥删掉 | 5 分钟 |

阶段 2 和 3 可能各有好几轮，每轮都是"AI 做、你验收"。

## 四、AI 让你做事时的几个固定套路

- **"照着点"**：AI 会写"打开哪个网址、点哪个按钮、选什么、填什么、看到什么算对"。照做就行，做不下去就截图发它。
- **填 Key 或密码**：AI 会告诉你填到哪个文件的哪一行。**不要把 Key 直接贴在对话里**，AI 也不应该把它复述出来。
- **问你花钱的事**：任何要付费的云资源，AI 创建前必须先告诉你大概多少钱，等你说"可以"。没说就别让它建。
- **让你打分**：它给你几条真实生成结果和一张评分表，你按自己的标准打分，标出"不能接受"的。这一步决定了后面什么算合格，认真打。

## 五、每个阶段结束，你怎么判断 AI 没糊弄

AI 每个阶段结束必须交**证据包**，只有三样，都放在 `docs/evidence/` 里：

1. 截图；
2. 一个你能自己打开的地址，或者一套"照着点"的步骤；
3. 一张不超过 10 行的是否表（测试跑没跑、真实模型调没调、密钥有没有排除、数据重启后在不在……）。

你做的事：打开地址点一遍，和截图对一下，看是否表有没有"否"。有"否"AI 就不能说完成，你可以直接回它"是否表第 X 项是否，先解决"。

## 六、中途停了、换工具、卡住了

- **隔天再来 / 换个窗口 / 换个 AI 工具或模型**：打开同一个文件夹，发第二节那句话。AI 会读 `docs/项目状态.md` 接着做。
- **AI 卡住反复报错**：把完整报错截图发它，说"按手册排错清单处理"。
- **AI 一次问太多**：回"一次最多 5 个，先问最重要的"。
- **AI 直接开始做你没同意的事**：回"停，先按规则卡问我确认"。

## 七、常见问题

**要花钱吗？** 两处：模型 Key（按用量付费，AI 会帮你估）；上线的云费用（AI 会在上线前给你估，创建前必须经你确认）。开发阶段可以先不买 Key，AI 会用模拟数据推进，验收前再买。

**必须企业认证吗？** 正式上线的默认方案需要公司做云平台企业认证。没有的话，AI 会先用临时方案上线并明确告诉你三条限制（只能一个实例、可能丢几分钟数据、不能扩容），认证办好后再切换。

**AI 说做完了，我怎么信？** 看第五节。没有证据包、是否表有"否"，就不算完成。

**可以中途换模型吗？** 可以。规则和进度都在文件夹里，不在某个模型的记忆里。

## 八、文件夹里有什么（不用打开，知道就行）

| 位置 | 是什么 | 谁用 |
| --- | --- | --- |
| `开始看这里.md` | 本手册 | 你 |
| `AGENTS.md`、`CLAUDE.md` | AI 的规则卡（CLAUDE.md 只是给 Claude Code 的引用） | AI |
| `docs/PRD/` | 你的 PRD 放这里；附一份模板可参考 | 你 |
| `docs/手册/` | 三份技术手册 | AI |
| `docs/项目状态.md` | 进度和决策记录，AI 自动创建维护 | AI，你可随时看 |
| `docs/evidence/` | 每阶段的截图和是否表，AI 自动创建 | 你核对用 |
| `backend/`、`frontend/` | AI 写的代码，之后会出现 | AI |
| `.gitignore` | 防止密钥被上传的配置 | AI |
