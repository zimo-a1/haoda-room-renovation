# 安全边界与费用

此仓库是 2026-09-21 当前工作版本的独立上传副本。原项目和旧 Git 历史保留在开发者本地；为避免携带历史凭证，上传副本从新的首次提交开始，不导入旧分支或标签。

## 下载与运行

- 不含项目所有者的 API Key、后端 `.env`、数据库、用户照片、真实生成结果或验收截图。
- 后端 `.env.example` 默认 `IMAGE_PROVIDER=mock`，仅运行明确标注的本地模拟，不调用真实模型。真实生成必须由运行者填写自己的密钥和费用配置。
- 私有仓库用于代码备份与受控协作，不是公开网站，不开启 GitHub Pages、自动部署或付费 GitHub Actions。
- 任何密钥都不能进入前端、提交历史、截图或日志。`.gitignore` 不能消除已提交的敏感内容。

## 不适合直接公开真实生成接口

当前为本地 MVP，尚无完整登录鉴权、用户数据隔离、每人额度、频率限制和并发原子预算预留。现有费用上限按估算记账，不保证供应商实际扣费绝不超额。不要将带真实 Key 的服务直接暴露到公网。

对外只分享不连接后端的静态演示。要开放真实生成，须先补齐上述保护，并在供应商侧配置可用的硬额度/告警（以平台实际能力为准）。

## 已知历史凭证事项

旧项目的历史 `.env.example` 曾含 SiliconFlow 凭证；上传副本不包含该历史或凭证。所有者仍应在原供应商控制台撤销或轮换旧密钥，清理副本不能替代撤销。旧本地 Git 仓库不要直接 push 到本仓库。

## 上传前检查

运行 `python3 scripts/check-upload-security.py`，检查当前文件和可达 Git 历史中的常见凭证格式、配置示例及敏感路径。脚本不打印凭证值。此检查不等于完整安全审计，也不能确认服务商密钥是否已撤销。

后续同步在本上传副本中复制审核过的代码，再检查、提交、推送；不要复制原项目的 `.git`、`.env`、`data` 或打包文件。
